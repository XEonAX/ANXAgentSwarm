# Development Dockerfile for Web (with hot reload)
FROM node:22-alpine
WORKDIR /app

# Copy package files
COPY src/ANXAgentSwarm.Web/package*.json ./

# Install dependencies
RUN npm ci

# Expose Vite dev server port
EXPOSE 5173

# Start development server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
